language: python
python:
  - 2.7
  - 3.4
  - 3.5
  - 3.5-dev
  - 3.6
  - 3.6-dev
  - nightly
  - pypy
install:
  - pip install -U pip wheel
  - make install-dev
  - pip install -U coveralls
script:
  - make clean docs test
after_success:
  - coveralls
